{% extends "base.html.twig" %}

{% block body %}

<h1 class="text-center mt-3">Ajouter une tâche</h1>
<div class="container mt-5" id="enregistrement" style="width: 500px">
    <div class="form-group">

        {{ form_start(form) }}

            <div class="form-group" style="width: 400px">
                {{ form_row(form.titre) }}
            </div>
            <div class="form-group" style="width: 180px">
                {{ form_row(form.date) }}
            </div>
            <div class="form-group" style="width: 400px">
                {{ form_row(form.detail) }}
            </div>
            <div class="text-center">
                <button class="btn btn-primary mt-3" type="submit">Enregistrer</button>
            </div>

        {{ form_end(form) }}
    </div>
</div>




{% endblock %}